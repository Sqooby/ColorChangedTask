/// User-visible copy for the app (single place, no scattered literals).
abstract final class UiStrings {
  UiStrings._();

  static const appName = 'Random Color';

  static const generateColor = 'Generate';
}
